import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    primary: {
      main: '#583DA1',
      contrastText: '#fff',
    },
    secondary: {
      main: '#2560A7',
      contrastText: '#fff',
    },
  },
})

export default theme
